const add = (a, b) => {
    const sum = a + b;
    console.log(`Addition: ${sum}`)
}

const subtract = (a, b) => {
    const sum2 = a - b;
    console.log(`Subtract: ${sum2}`)
}

module.exports = {add, subtract};